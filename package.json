{
  "name": "gamevault-x-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm start --prefix backend",
    "seed": "node backend/seed.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
